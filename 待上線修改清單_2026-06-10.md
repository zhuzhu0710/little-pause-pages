# Little Pause Pages 待上線修改清單

更新日期：2026-07-02

## 目前狀態

- Netlify 點數尚未恢復。
- 先在本機離線調整，確認沒有問題後，再一次性更新上線。

## 已完成的方向

- 首頁 `index.html` 已改成目前品牌與首本書的定位。
- `Books` 頁已改成顯示真實封面 `First Words Around Me`。
- `Free Samples` 頁已改成「預覽圖卡＋留下 Email 索取 PDF」，不提供站上直接下載。
- `Contact` 頁已改成 Netlify 表單，送出後會顯示同頁確認訊息。
- `FAQ`、`About`、`Privacy` 內容已朝目前產品與 Email 流程調整。

## 上線前要再確認的項目

1. `index.html`、`about.html`、`books.html`、`faq.html`、`contact.html`、`privacy.html` 的文案是否一致。
2. `samples.html` 是否只保留預覽，不出現直接下載 PDF 的入口。
3. `samples.html` 的表單送出後，是否能顯示 `Request sent` 的確認區塊。
4. `contact.html` 的表單送出後，是否能顯示 `Message sent` 的確認區塊。
5. `assets/first-words-around-me-cover.png` 是否已正確放入 `books.html`。
6. 是否還有舊的 `free-samples-thanks` 連結殘留。
7. `styles.css` 的整體風格是否和新品牌一致。

## 小提醒

- 目前 `little.pause.pages@gmail.com` 仍需要人工查看。
- 之後若要改成自動檢查信箱，再另外串接 Gmail。
- GitHub Desktop 的提交摘要可以先寫成：
  `Update Little Pause Pages site copy, samples flow, book cover, FAQ, contact, privacy, and homepage branding`
